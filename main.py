# Este é o arquivo principal. Execute-o para rodar a aplicação.

from application import Sistema

if __name__ == '__main__':
    sist = Sistema(reset=False) # Parâmetro Reset: Se TRUE, reseta o conteúdo das tabelas
